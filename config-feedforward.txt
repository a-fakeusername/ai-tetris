[NEAT]
fitness_criterion     = max
fitness_threshold     = 0
pop_size              = 100
reset_on_extinction   = False

[DefaultGenome]
# num_inputs, num_hidden, num_outputs are key for environment integration
num_inputs              = 15  # CartPole observation space size
num_hidden              = 0  # Can start with 0 hidden, NEAT will add them
num_outputs             = 2  # CartPole action space size (probabilities for action 0 or 1)
                            # OR 1 if using a single output and thresholding for binary action

initial_connection      = full_direct
feed_forward            = True
compatibility_disjoint_coefficient = 1.0
compatibility_weight_coefficient = 0.6

conn_add_prob           = 0.1
conn_delete_prob        = 0.1
node_add_prob           = 0.1
node_delete_prob        = 0.05

# Activation functions
activation_default      = relu # common choice
activation_mutate_rate  = 0.1
activation_options      = relu sigmoid tanh

# Other parameters can be tuned
# ... (see neat-python documentation for all options)

[DefaultSpeciesSet]
compatibility_threshold = 3.0

[DefaultStagnation]
species_fitness_func = max
max_stagnation       = 20
species_elitism      = 2